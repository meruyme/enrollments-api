version: "3.9"

services:
  api:
    container_name: enrollments-api
    build:
      context: ./
      dockerfile: Dockerfile
    volumes:
      - .:/code
    env_file: .env
    ports:
      - "8000:8000"
    depends_on:
      - age-group-api
      - mongo
      - rabbitmq
    networks:
      - enrollments-network

  age-group-api:
    container_name: age-group-api
    build:
      context: ${AGE_GROUP_PROJECT_PATH}
      dockerfile: Dockerfile
    volumes:
      - ${AGE_GROUP_PROJECT_PATH}:/code
    env_file:
      - ${AGE_GROUP_PROJECT_PATH}/.env
    ports:
      - "8001:8000"
    depends_on:
      - mongo
    networks:
      - enrollments-network

  mongo:
    container_name: mongo
    image: mongo
    restart: always
    volumes:
      - mongo_data:/data/db
    ports:
      - "27017:27017"
    networks:
      - enrollments-network

  mongo-express:
    container_name: mongo-express
    image: mongo-express
    restart: always
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_SERVER: mongo
      ME_CONFIG_BASICAUTH: false
    depends_on:
      - mongo
    networks:
      - enrollments-network

  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - enrollments-network

volumes:
  mongo_data:
  rabbitmq_data:

networks:
    enrollments-network:
      driver: bridge
